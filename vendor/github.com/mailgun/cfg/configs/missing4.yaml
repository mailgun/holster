key: val

env: {{.Env.SOME_ENV_KEY}}

bool: true

builtin:
  anotherkey: anothervalue
  mandatory: onemorevalue
  onemorebuiltin:
    blah: blah

number: 42

ptrstruct:
  ptrblah: blah
  ptroptional: optional

ptroptional:
  ptrfoo: blah

